# Prevent directory listing
Options -Indexes

# Block direct access to non-PHP files (README, docs, etc.)
<FilesMatch "\.(md|txt|sql|bak|log)$">
    Require all denied
</FilesMatch>

# Block direct access to template scripts
<FilesMatch "^_TEMPLATE">
    Require all denied
</FilesMatch>
