# Disable directory browsing for security
Options -Indexes

# Allow index.php access for admin interface
# Block direct access to PHP fix scripts (they should be run via index.php)
<FilesMatch "^(?!index\.php$).*\.php$">
    Require all denied
</FilesMatch>